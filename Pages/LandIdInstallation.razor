@page "/land-id/installation"
@page "/{culture}/land-id/installation"

<InstallPageTemplate PageKey="land-id-installation" RoutePath="land-id/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
