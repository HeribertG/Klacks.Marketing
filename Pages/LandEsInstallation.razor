@page "/land-es/installation"
@page "/{culture}/land-es/installation"

<InstallPageTemplate PageKey="land-es-installation" RoutePath="land-es/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
