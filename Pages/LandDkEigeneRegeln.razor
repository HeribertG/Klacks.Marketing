@page "/land-dk/eigene-regeln"
@page "/{culture}/land-dk/eigene-regeln"

<IndustryPageTemplate PageKey="land-dk-eigene-regeln" RoutePath="land-dk/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
