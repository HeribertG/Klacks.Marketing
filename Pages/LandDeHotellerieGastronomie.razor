@page "/land-de/hotellerie-gastronomie"
@page "/{culture}/land-de/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-de-hotellerie-gastronomie" RoutePath="land-de/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
