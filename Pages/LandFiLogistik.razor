@page "/land-fi/logistik"
@page "/{culture}/land-fi/logistik"

<IndustryPageTemplate PageKey="land-fi-logistik" RoutePath="land-fi/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
