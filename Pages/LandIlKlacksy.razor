@page "/land-il/klacksy"
@page "/{culture}/land-il/klacksy"

<IndustryPageTemplate PageKey="land-il-klacksy" RoutePath="land-il/klacksy" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
