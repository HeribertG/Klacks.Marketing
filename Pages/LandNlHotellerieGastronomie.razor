@page "/land-nl/hotellerie-gastronomie"
@page "/{culture}/land-nl/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-nl-hotellerie-gastronomie" RoutePath="land-nl/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
