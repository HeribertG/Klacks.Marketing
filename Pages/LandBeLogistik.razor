@page "/land-be/logistik"
@page "/{culture}/land-be/logistik"

<IndustryPageTemplate PageKey="land-be-logistik" RoutePath="land-be/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
