@page "/land-gr/installation"
@page "/{culture}/land-gr/installation"

<InstallPageTemplate PageKey="land-gr-installation" RoutePath="land-gr/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
