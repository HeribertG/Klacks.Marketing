@page "/land-ie/logistik"
@page "/{culture}/land-ie/logistik"

<IndustryPageTemplate PageKey="land-ie-logistik" RoutePath="land-ie/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
