@page "/land-it/eigene-regeln"
@page "/{culture}/land-it/eigene-regeln"

<IndustryPageTemplate PageKey="land-it-eigene-regeln" RoutePath="land-it/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
