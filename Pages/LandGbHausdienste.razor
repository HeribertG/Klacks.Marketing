@page "/land-gb/hausdienste"
@page "/{culture}/land-gb/hausdienste"

<IndustryPageTemplate PageKey="land-gb-hausdienste" RoutePath="land-gb/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
