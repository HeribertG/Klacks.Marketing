@page "/land-th/eigene-regeln"
@page "/{culture}/land-th/eigene-regeln"

<IndustryPageTemplate PageKey="land-th-eigene-regeln" RoutePath="land-th/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
