@page "/land-cz/installation"
@page "/{culture}/land-cz/installation"

<InstallPageTemplate PageKey="land-cz-installation" RoutePath="land-cz/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
