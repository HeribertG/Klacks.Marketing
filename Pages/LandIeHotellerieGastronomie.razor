@page "/land-ie/hotellerie-gastronomie"
@page "/{culture}/land-ie/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-ie-hotellerie-gastronomie" RoutePath="land-ie/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
