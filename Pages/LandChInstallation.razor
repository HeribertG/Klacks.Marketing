@page "/land-ch/installation"
@page "/{culture}/land-ch/installation"

<InstallPageTemplate PageKey="land-ch-installation" RoutePath="land-ch/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
