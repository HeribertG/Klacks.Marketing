@page "/land-dk/spitex"
@page "/{culture}/land-dk/spitex"

<IndustryPageTemplate PageKey="land-dk-spitex" RoutePath="land-dk/spitex" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
