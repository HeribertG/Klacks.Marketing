@page "/land-ch/logistik"
@page "/{culture}/land-ch/logistik"

<IndustryPageTemplate PageKey="land-ch-logistik" RoutePath="land-ch/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
