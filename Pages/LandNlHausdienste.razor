@page "/land-nl/hausdienste"
@page "/{culture}/land-nl/hausdienste"

<IndustryPageTemplate PageKey="land-nl-hausdienste" RoutePath="land-nl/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
