@page "/land-vn/hausdienste"
@page "/{culture}/land-vn/hausdienste"

<IndustryPageTemplate PageKey="land-vn-hausdienste" RoutePath="land-vn/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
