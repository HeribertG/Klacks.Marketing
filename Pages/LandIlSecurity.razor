@page "/land-il/security"
@page "/{culture}/land-il/security"

<IndustryPageTemplate PageKey="land-il-security" RoutePath="land-il/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
