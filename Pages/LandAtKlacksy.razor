@page "/land-at/klacksy"
@page "/{culture}/land-at/klacksy"

<IndustryPageTemplate PageKey="land-at-klacksy" RoutePath="land-at/klacksy" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
