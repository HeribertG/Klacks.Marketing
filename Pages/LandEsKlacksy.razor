@page "/land-es/klacksy"
@page "/{culture}/land-es/klacksy"

<IndustryPageTemplate PageKey="land-es-klacksy" RoutePath="land-es/klacksy" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
