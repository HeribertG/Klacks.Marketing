@page "/land-ro/hotellerie-gastronomie"
@page "/{culture}/land-ro/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-ro-hotellerie-gastronomie" RoutePath="land-ro/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
