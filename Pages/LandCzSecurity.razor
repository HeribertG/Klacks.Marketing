@page "/land-cz/security"
@page "/{culture}/land-cz/security"

<IndustryPageTemplate PageKey="land-cz-security" RoutePath="land-cz/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
