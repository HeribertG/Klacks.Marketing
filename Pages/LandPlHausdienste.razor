@page "/land-pl/hausdienste"
@page "/{culture}/land-pl/hausdienste"

<IndustryPageTemplate PageKey="land-pl-hausdienste" RoutePath="land-pl/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
