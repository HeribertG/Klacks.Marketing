@page "/land-cn/logistik"
@page "/{culture}/land-cn/logistik"

<IndustryPageTemplate PageKey="land-cn-logistik" RoutePath="land-cn/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
