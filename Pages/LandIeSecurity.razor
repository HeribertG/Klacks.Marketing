@page "/land-ie/security"
@page "/{culture}/land-ie/security"

<IndustryPageTemplate PageKey="land-ie-security" RoutePath="land-ie/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
