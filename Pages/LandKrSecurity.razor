@page "/land-kr/security"
@page "/{culture}/land-kr/security"

<IndustryPageTemplate PageKey="land-kr-security" RoutePath="land-kr/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
