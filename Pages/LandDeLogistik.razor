@page "/land-de/logistik"
@page "/{culture}/land-de/logistik"

<IndustryPageTemplate PageKey="land-de-logistik" RoutePath="land-de/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
