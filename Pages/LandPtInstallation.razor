@page "/land-pt/installation"
@page "/{culture}/land-pt/installation"

<InstallPageTemplate PageKey="land-pt-installation" RoutePath="land-pt/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
