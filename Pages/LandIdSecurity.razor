@page "/land-id/security"
@page "/{culture}/land-id/security"

<IndustryPageTemplate PageKey="land-id-security" RoutePath="land-id/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
