@page "/land-es/hotellerie-gastronomie"
@page "/{culture}/land-es/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-es-hotellerie-gastronomie" RoutePath="land-es/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
