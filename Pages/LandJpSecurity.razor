@page "/land-jp/security"
@page "/{culture}/land-jp/security"

<IndustryPageTemplate PageKey="land-jp-security" RoutePath="land-jp/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
