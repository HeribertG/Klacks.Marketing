@page "/land-fr/spitex"
@page "/{culture}/land-fr/spitex"

<IndustryPageTemplate PageKey="land-fr-spitex" RoutePath="land-fr/spitex" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
