@page "/land-th/spitex"
@page "/{culture}/land-th/spitex"

<IndustryPageTemplate PageKey="land-th-spitex" RoutePath="land-th/spitex" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
