@page "/land-dk/installation"
@page "/{culture}/land-dk/installation"

<InstallPageTemplate PageKey="land-dk-installation" RoutePath="land-dk/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
