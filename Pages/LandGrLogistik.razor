@page "/land-gr/logistik"
@page "/{culture}/land-gr/logistik"

<IndustryPageTemplate PageKey="land-gr-logistik" RoutePath="land-gr/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
