@page "/land-il/logistik"
@page "/{culture}/land-il/logistik"

<IndustryPageTemplate PageKey="land-il-logistik" RoutePath="land-il/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
