@page "/land-pl/spitex"
@page "/{culture}/land-pl/spitex"

<IndustryPageTemplate PageKey="land-pl-spitex" RoutePath="land-pl/spitex" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
