@page "/land-kr/installation"
@page "/{culture}/land-kr/installation"

<InstallPageTemplate PageKey="land-kr-installation" RoutePath="land-kr/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
