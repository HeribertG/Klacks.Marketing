@page "/land-dk/hausdienste"
@page "/{culture}/land-dk/hausdienste"

<IndustryPageTemplate PageKey="land-dk-hausdienste" RoutePath="land-dk/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
