@page "/land-se/hausdienste"
@page "/{culture}/land-se/hausdienste"

<IndustryPageTemplate PageKey="land-se-hausdienste" RoutePath="land-se/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
