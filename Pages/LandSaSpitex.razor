@page "/land-sa/spitex"
@page "/{culture}/land-sa/spitex"

<IndustryPageTemplate PageKey="land-sa-spitex" RoutePath="land-sa/spitex" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
