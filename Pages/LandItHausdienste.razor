@page "/land-it/hausdienste"
@page "/{culture}/land-it/hausdienste"

<IndustryPageTemplate PageKey="land-it-hausdienste" RoutePath="land-it/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
