@page "/land-no/installation"
@page "/{culture}/land-no/installation"

<InstallPageTemplate PageKey="land-no-installation" RoutePath="land-no/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
