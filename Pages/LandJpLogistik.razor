@page "/land-jp/logistik"
@page "/{culture}/land-jp/logistik"

<IndustryPageTemplate PageKey="land-jp-logistik" RoutePath="land-jp/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
