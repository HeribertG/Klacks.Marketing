@page "/land-no/logistik"
@page "/{culture}/land-no/logistik"

<IndustryPageTemplate PageKey="land-no-logistik" RoutePath="land-no/logistik" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
