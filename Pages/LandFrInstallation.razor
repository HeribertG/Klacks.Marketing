@page "/land-fr/installation"
@page "/{culture}/land-fr/installation"

<InstallPageTemplate PageKey="land-fr-installation" RoutePath="land-fr/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
