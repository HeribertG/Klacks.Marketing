@page "/land-sa/hotellerie-gastronomie"
@page "/{culture}/land-sa/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-sa-hotellerie-gastronomie" RoutePath="land-sa/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
