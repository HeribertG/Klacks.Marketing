@page "/land-vn/eigene-regeln"
@page "/{culture}/land-vn/eigene-regeln"

<IndustryPageTemplate PageKey="land-vn-eigene-regeln" RoutePath="land-vn/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
