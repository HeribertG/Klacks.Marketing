@page "/land-tw/eigene-regeln"
@page "/{culture}/land-tw/eigene-regeln"

<IndustryPageTemplate PageKey="land-tw-eigene-regeln" RoutePath="land-tw/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
