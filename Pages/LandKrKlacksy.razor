@page "/land-kr/klacksy"
@page "/{culture}/land-kr/klacksy"

<IndustryPageTemplate PageKey="land-kr-klacksy" RoutePath="land-kr/klacksy" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
