@page "/land-ro/eigene-regeln"
@page "/{culture}/land-ro/eigene-regeln"

<IndustryPageTemplate PageKey="land-ro-eigene-regeln" RoutePath="land-ro/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
