@page "/land-tw/spitex"
@page "/{culture}/land-tw/spitex"

<IndustryPageTemplate PageKey="land-tw-spitex" RoutePath="land-tw/spitex" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
