@page "/land-jp/hotellerie-gastronomie"
@page "/{culture}/land-jp/hotellerie-gastronomie"

<IndustryPageTemplate PageKey="land-jp-hotellerie-gastronomie" RoutePath="land-jp/hotellerie-gastronomie" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
