@page "/land-no/hausdienste"
@page "/{culture}/land-no/hausdienste"

<IndustryPageTemplate PageKey="land-no-hausdienste" RoutePath="land-no/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
