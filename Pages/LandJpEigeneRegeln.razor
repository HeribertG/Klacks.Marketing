@page "/land-jp/eigene-regeln"
@page "/{culture}/land-jp/eigene-regeln"

<IndustryPageTemplate PageKey="land-jp-eigene-regeln" RoutePath="land-jp/eigene-regeln" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
