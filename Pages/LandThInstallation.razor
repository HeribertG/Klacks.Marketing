@page "/land-th/installation"
@page "/{culture}/land-th/installation"

<InstallPageTemplate PageKey="land-th-installation" RoutePath="land-th/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
