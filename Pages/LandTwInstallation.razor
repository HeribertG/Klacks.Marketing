@page "/land-tw/installation"
@page "/{culture}/land-tw/installation"

<InstallPageTemplate PageKey="land-tw-installation" RoutePath="land-tw/installation" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
