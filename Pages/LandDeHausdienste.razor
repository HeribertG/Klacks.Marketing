@page "/land-de/hausdienste"
@page "/{culture}/land-de/hausdienste"

<IndustryPageTemplate PageKey="land-de-hausdienste" RoutePath="land-de/hausdienste" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
