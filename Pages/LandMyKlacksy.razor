@page "/land-my/klacksy"
@page "/{culture}/land-my/klacksy"

<IndustryPageTemplate PageKey="land-my-klacksy" RoutePath="land-my/klacksy" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
