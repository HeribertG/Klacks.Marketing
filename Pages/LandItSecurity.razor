@page "/land-it/security"
@page "/{culture}/land-it/security"

<IndustryPageTemplate PageKey="land-it-security" RoutePath="land-it/security" Culture="@ResolvedCulture" />

@code {
    [Parameter] public string? Culture { get; set; }

    private string ResolvedCulture => SupportedCultures.Resolve(Culture).Code;
}
